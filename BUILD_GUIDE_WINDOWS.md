# 📖 Подробное руководство по сборке WeTax с Windows

Это руководство поможет вам собрать iOS приложение WeTax **без прямого доступа к Mac**.

---

## 🎯 Обзор методов сборки

| Метод | Сложность | Стоимость | Время сборки | Рекомендация |
|-------|-----------|-----------|--------------|--------------|
| GitHub Actions | ⭐ Легко | Бесплатно | ~10-15 мин | ⭐⭐⭐⭐⭐ |
| Codemagic | ⭐ Легко | 500 мин/мес бесплатно | ~5-10 мин | ⭐⭐⭐⭐⭐ |
| Fastlane + CI | ⭐⭐ Средне | Зависит от CI | ~10-20 мин | ⭐⭐⭐⭐ |
| Облачный Mac | ⭐⭐⭐ Сложно | $50-100/мес | Зависит от вас | ⭐⭐⭐ |
| React Native | ⭐⭐ Средне | Бесплатно | ~5-10 мин | ⭐⭐⭐ |

---

## 🚀 Метод 1: GitHub Actions (Самый простой)

### Шаг 1: Подготовка проекта

1. **Убедитесь, что все файлы на месте:**
   ```
   WeTax/
   ├── WeTax/
   │   ├── WeTaxApp.swift
   │   ├── ContentView.swift
   │   └── ... (все остальные файлы)
   ├── WeTax.xcodeproj/
   ├── .github/
   │   └── workflows/
   │       └── ios-build.yml
   └── README.md
   ```

2. **Инициализируйте Git репозиторий:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit: WeTax iOS app"
   ```

### Шаг 2: Создание GitHub репозитория

1. **Создайте новый репозиторий на GitHub:**
   - Перейдите на https://github.com/new
   - Название: `wetax-ios`
   - Выберите "Public" (для бесплатного использования Actions)
   - НЕ создавайте README, .gitignore или лицензию

2. **Подключите локальный репозиторий:**
   ```bash
   git remote add origin https://github.com/wetakush/wetax-ios.git
   git branch -M main
   git push -u origin main
   ```

### Шаг 3: Запуск сборки

1. **Автоматический запуск:**
   - GitHub Actions запустится автоматически при push в `main` или `develop`

2. **Ручной запуск:**
   - Перейдите в репозиторий → вкладка "Actions"
   - Выберите "iOS Build" workflow
   - Нажмите "Run workflow" → "Run workflow"

### Шаг 4: Мониторинг сборки

1. **Откройте вкладку "Actions":**
   - Вы увидите список всех запусков
   - Желтый кружок = в процессе
   - Зеленый ✓ = успешно
   - Красный ✗ = ошибка

2. **Просмотр логов:**
   - Кликните на workflow run
   - Разверните каждый шаг для просмотра логов

### Шаг 5: Скачивание артефактов

1. **После успешной сборки:**
   - Внизу страницы workflow run найдите раздел "Artifacts"
   - Нажмите "WeTax-build"
   - Скачайте ZIP архив

2. **Распакуйте архив:**
   - Внутри будет папка `build/` с результатами сборки

### ⚠️ Решение проблем

**Проблема:** Сборка падает с ошибкой "Code signing is required"
- **Решение:** Это нормально для симулятора. Проверьте, что в workflow используется `CODE_SIGNING_REQUIRED=NO`

**Проблема:** Не могу найти артефакты
- **Решение:** Артефакты доступны только после успешной сборки. Проверьте, что все шаги завершились успешно.

**Проблема:** Сборка занимает слишком долго
- **Решение:** Это нормально. Первая сборка может занять 15-20 минут из-за установки зависимостей.

---

## 🚀 Метод 2: Codemagic (Самый быстрый)

### Шаг 1: Регистрация

1. **Создайте аккаунт:**
   - Перейдите на https://codemagic.io/signup
   - Войдите через GitHub (рекомендуется)

2. **Подтвердите email:**
   - Проверьте почту и подтвердите регистрацию

### Шаг 2: Подключение репозитория

1. **Добавьте приложение:**
   - Нажмите "Add application"
   - Выберите "GitHub" как источник
   - Выберите репозиторий `wetax-ios`
   - Выберите платформу "iOS"

2. **Настройте проект:**
   - Codemagic автоматически обнаружит `codemagic.yaml`
   - Если нет, выберите "Configure workflow manually"

### Шаг 3: Первая сборка

1. **Запустите сборку:**
   - Нажмите "Start new build"
   - Выберите ветку `main`
   - Нажмите "Start build"

2. **Мониторинг:**
   - Следите за прогрессом в реальном времени
   - Видео-логи показывают каждый шаг

### Шаг 4: Скачивание IPA

1. **После завершения:**
   - В разделе "Artifacts" найдите `.ipa` файл
   - Скачайте его на компьютер

2. **Установка на устройство:**
   - Для установки требуется подпись кода (см. раздел "Подпись кода")

### ⚠️ Решение проблем

**Проблема:** "No valid code signing certificates found"
- **Решение:** Для тестирования на симуляторе подпись не нужна. Для устройства - настройте сертификаты (см. ниже)

**Проблема:** Превышен лимит бесплатных минут
- **Решение:** Подождите до следующего месяца или перейдите на платный план

---

## 🔐 Настройка подписи кода (для установки на устройство)

### Вариант A: Через Codemagic (Рекомендуется)

1. **Создайте Apple Developer аккаунт:**
   - Перейдите на https://developer.apple.com
   - Зарегистрируйтесь ($99/год)

2. **Создайте сертификаты:**
   - В Codemagic: Settings → Code signing identities
   - Нажмите "Add code signing identity"
   - Следуйте инструкциям

3. **Обновите `codemagic.yaml`:**
   ```yaml
   environment:
     groups:
       - app_store_credentials
   ```

### Вариант B: Через Fastlane Match

1. **Установите Fastlane:**
   ```bash
   # На Mac или через облачный Mac
   sudo gem install fastlane
   ```

2. **Инициализируйте Match:**
   ```bash
   fastlane match init
   ```

3. **Создайте сертификаты:**
   ```bash
   fastlane match development
   fastlane match appstore
   ```

---

## 🧪 Тестирование собранного приложения

### Метод 1: BrowserStack App Live

1. **Зарегистрируйтесь:**
   - https://www.browserstack.com/app-live
   - Бесплатный пробный период доступен

2. **Загрузите IPA:**
   - Используйте IPA, собранный через Codemagic
   - Загрузите через веб-интерфейс BrowserStack

3. **Тестируйте:**
   - Доступ к реальным iPhone через браузер
   - Тестирование на разных версиях iOS

### Метод 2: TestFlight

1. **Требования:**
   - Apple Developer аккаунт ($99/год)
   - Подписанное приложение

2. **Загрузка в App Store Connect:**
   - Соберите IPA через Codemagic с подписью
   - Загрузите через Transporter или Xcode
   - Добавьте в TestFlight

3. **Тестирование:**
   - Добавьте тестеров в TestFlight
   - Они получат приглашение на email
   - Установят через TestFlight app

### Метод 3: Локальная установка (требует Mac)

Если у вас есть доступ к Mac (даже удаленный):

1. **Установите Xcode:**
   ```bash
   # Через App Store или xcode-select
   ```

2. **Откройте проект:**
   ```bash
   open WeTax/WeTax.xcodeproj
   ```

3. **Подключите устройство:**
   - Через USB или беспроводную отладку
   - В Xcode выберите устройство
   - Нажмите "Run"

---

## 📊 Сравнение методов

### GitHub Actions

**Плюсы:**
- ✅ Полностью бесплатно для публичных репозиториев
- ✅ Неограниченное количество сборок
- ✅ Интеграция с GitHub
- ✅ Хорошо документировано

**Минусы:**
- ❌ Медленнее чем Codemagic
- ❌ Меньше возможностей для iOS
- ❌ Нет автоматического деплоя в App Store

### Codemagic

**Плюсы:**
- ✅ Быстрая сборка (5-10 минут)
- ✅ Отличная поддержка iOS
- ✅ Автоматический деплой в App Store
- ✅ Видео-логи сборки
- ✅ 500 бесплатных минут/месяц

**Минусы:**
- ❌ Ограниченное количество бесплатных минут
- ❌ Платные планы дороже GitHub Actions

---

## 🛠 Продвинутые настройки

### Настройка автоматических сборок

**GitHub Actions - при каждом коммите:**
```yaml
# Уже настроено в .github/workflows/ios-build.yml
on:
  push:
    branches: [ main, develop ]
```

**Codemagic - по расписанию:**
```yaml
# Добавьте в codemagic.yaml
workflows:
  ios-workflow:
    triggers:
      - schedule:
          cron: "0 2 * * *"  # Каждый день в 2:00 UTC
```

### Настройка уведомлений

**Email уведомления (Codemagic):**
```yaml
publishing:
  email:
    recipients:
      - your-email@example.com
    notify:
      success: true
      failure: true
```

**Slack уведомления:**
```yaml
publishing:
  slack:
    channel: '#builds'
    notify:
      success: true
      failure: true
```

---

## 📝 Чеклист перед первой сборкой

- [ ] Все файлы проекта закоммичены в Git
- [ ] Репозиторий загружен на GitHub
- [ ] Файл `.github/workflows/ios-build.yml` присутствует
- [ ] Файл `codemagic.yaml` присутствует (если используете Codemagic)
- [ ] Bundle ID настроен в `Info.plist` (com.wetax.app)
- [ ] Минимальная версия iOS указана (16.0)

---

## 🆘 Часто задаваемые вопросы

**Q: Можно ли собрать iOS приложение на Windows без Mac?**
A: Нет, напрямую нельзя. Но можно использовать облачные сервисы (GitHub Actions, Codemagic) которые предоставляют Mac для сборки.

**Q: Сколько стоит сборка?**
A: GitHub Actions - бесплатно для публичных репозиториев. Codemagic - 500 минут бесплатно в месяц.

**Q: Как установить приложение на iPhone без Mac?**
A: Через TestFlight (требует Apple Developer аккаунт) или через BrowserStack для тестирования.

**Q: Можно ли использовать этот код для продакшена?**
A: Код готов к использованию, но требуется подключение к реальному бэкенду API вместо мок-данных.

**Q: Как добавить реальные push-уведомления?**
A: Требуется настройка APNs сертификатов в Apple Developer Portal и интеграция с Firebase или другим сервисом.

---

## 📚 Полезные ссылки

- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [Codemagic Documentation](https://docs.codemagic.io)
- [Apple Developer Portal](https://developer.apple.com)
- [Fastlane Documentation](https://docs.fastlane.tools)
- [SwiftUI Documentation](https://developer.apple.com/documentation/swiftui)

---

## 🎉 Готово!

Теперь вы знаете, как собрать iOS приложение WeTax с Windows. Выберите наиболее подходящий метод и начните сборку!

**Рекомендация:** Начните с GitHub Actions - это самый простой и бесплатный способ.

Удачи! 🚀

