# 📁 Структура проекта WeTax

## Обзор

Проект полностью написан на SwiftUI без использования Storyboard или Interface Builder. Весь UI создан кодом, что позволяет легко собирать проект в облачных CI/CD системах.

## Структура директорий

```
WeTax/
├── WeTax/                          # Основная директория приложения
│   ├── WeTaxApp.swift              # Точка входа приложения
│   ├── ContentView.swift            # Главный экран с навигацией
│   │
│   ├── Models/                     # Модели данных
│   │   ├── User.swift              # Модель пользователя
│   │   └── Ride.swift              # Модель поездки (включая CarType, RideStatus)
│   │
│   ├── ViewModels/                 # ViewModels (MVVM архитектура)
│   │   ├── AuthViewModel.swift     # Логика авторизации
│   │   └── RideViewModel.swift     # Логика управления поездками
│   │
│   ├── Views/                      # SwiftUI Views
│   │   ├── LoginView.swift         # Экран авторизации
│   │   ├── OrderTaxiView.swift     # Экран заказа такси (главный)
│   │   ├── MapView.swift           # Кастомная карта с маркерами
│   │   ├── WaitingForDriverView.swift  # Экран ожидания водителя
│   │   ├── RideHistoryView.swift   # История поездок
│   │   ├── ProfileView.swift       # Профиль пользователя
│   │   └── AddressInputView.swift  # Поиск и выбор адресов
│   │
│   ├── Services/                   # Сервисы
│   │   ├── LocationService.swift   # Работа с геолокацией (CoreLocation)
│   │   └── NotificationService.swift  # Push-уведомления
│   │
│   ├── Extensions/                 # Расширения
│   │   └── Color+Hex.swift         # Поддержка hex цветов
│   │
│   ├── Assets.xcassets/            # Ресурсы (иконки, цвета)
│   ├── Info.plist                  # Конфигурация приложения
│   └── Preview Content/            # Ресурсы для SwiftUI Preview
│
├── WeTax.xcodeproj/                # Xcode проект
│   └── project.pbxproj             # Конфигурация проекта
│
├── .github/                         # GitHub Actions
│   └── workflows/
│       └── ios-build.yml           # Автоматическая сборка
│
├── fastlane/                       # Fastlane конфигурация
│   ├── Fastfile                    # Скрипты автоматизации
│   └── Appfile                     # Настройки приложения
│
├── README.md                       # Основная документация
├── BUILD_GUIDE_WINDOWS.md          # Подробное руководство по сборке
├── QUICK_START.md                  # Быстрый старт
├── PROJECT_STRUCTURE.md            # Этот файл
├── codemagic.yaml                  # Конфигурация Codemagic
└── .gitignore                      # Игнорируемые файлы
```

## Архитектура

### MVVM (Model-View-ViewModel)

```
┌─────────┐         ┌──────────────┐         ┌─────────┐
│  View   │ ◄─────► │  ViewModel   │ ◄─────► │  Model  │
│(SwiftUI)│         │  (Logic)     │         │  (Data) │
└─────────┘         └──────────────┘         └─────────┘
     │                    │                       │
     │                    │                       │
     └────────────────────┴───────────────────────┘
                    Services
              (Location, Notifications)
```

### Поток данных

1. **View** отображает UI и обрабатывает пользовательский ввод
2. **ViewModel** содержит бизнес-логику и состояние
3. **Model** представляет данные
4. **Services** предоставляют функциональность (геолокация, уведомления)

## Основные компоненты

### Models

- **User**: Информация о пользователе (id, телефон, имя, email)
- **Ride**: Информация о поездке (адреса, тип авто, статус, цена, водитель)
- **CarType**: Тип автомобиля (Эконом, Комфорт, Бизнес)
- **RideStatus**: Статус поездки (поиск, найдено, в пути, завершена)
- **Driver**: Информация о водителе
- **LocationCoordinate**: Координаты для работы с картами

### ViewModels

- **AuthViewModel**: 
  - Управление авторизацией
  - Сохранение сессии
  - Валидация данных

- **RideViewModel**:
  - Создание и управление поездками
  - Расчет стоимости
  - Поиск водителя (симуляция)
  - История поездок

### Services

- **LocationService**:
  - Запрос разрешений на геолокацию
  - Получение текущего местоположения
  - Обновление координат в реальном времени

- **NotificationService**:
  - Запрос разрешений на уведомления
  - Отправка локальных уведомлений

### Views

- **LoginView**: Экран входа с полями телефона и пароля
- **OrderTaxiView**: Главный экран с картой и панелями заказа
- **MapView**: Кастомная карта с маркерами и маршрутами
- **WaitingForDriverView**: Экран ожидания с информацией о водителе
- **RideHistoryView**: Список завершенных поездок
- **ProfileView**: Профиль пользователя и настройки
- **AddressInputView**: Поиск адресов через MapKit

## Зависимости

Проект использует только стандартные фреймворки Apple:
- ✅ SwiftUI (UI)
- ✅ MapKit (карты)
- ✅ CoreLocation (геолокация)
- ✅ UserNotifications (уведомления)
- ✅ Combine (реактивное программирование)

**Нет внешних зависимостей** - проект готов к сборке без `pod install` или `carthage`.

## Конфигурация

### Info.plist

Содержит необходимые разрешения:
- `NSLocationWhenInUseUsageDescription` - для геолокации
- `NSLocationAlwaysAndWhenInUseUsageDescription` - для отслеживания поездки

### Xcode Project

- **Bundle ID**: `com.wetax.app`
- **Минимальная версия iOS**: 16.0
- **Язык**: Swift 5
- **Deployment Target**: iOS 16.0+

## Особенности реализации

### Геолокация
- Автоматическое определение местоположения при запуске
- Запрос разрешений с понятными описаниями
- Обработка ошибок доступа

### Карты
- Интерактивная карта с пользовательским местоположением
- Маркеры для точек отправления и назначения
- Построение маршрута между точками
- Отслеживание водителя в реальном времени

### Данные
- Используется `UserDefaults` для локального хранения
- В продакшене требуется подключение к API
- Мок-данные для демонстрации функционала

### Навигация
- Tab-based navigation для основных экранов
- Sheet presentations для модальных окон
- NavigationView для иерархической навигации

## Цветовая схема

Корпоративные цвета WeTax:
- **Основной**: #007AFF (синий)
- **Фон**: Белый
- **Акценты**: Красный (для точки назначения), Зеленый (для водителя)

## Адаптивность

Приложение адаптировано для:
- ✅ iPhone (все размеры)
- ✅ iPad (базовая поддержка)
- ✅ Разные ориентации (Portrait, Landscape)

## Готовность к продакшену

### ✅ Готово:
- Полная структура приложения
- Все основные экраны
- Базовая функциональность
- Геолокация и карты
- История поездок
- Профиль пользователя

### ⚠️ Требует доработки:
- Подключение к реальному API
- Реальные push-уведомления (APNs)
- Интеграция платежных систем
- Тестирование на реальных устройствах
- Оптимизация производительности

## Расширение функционала

Для добавления новых возможностей:

1. **Новый экран**: Создайте файл в `Views/`
2. **Новая модель**: Добавьте в `Models/`
3. **Новая логика**: Создайте ViewModel в `ViewModels/`
4. **Новый сервис**: Добавьте в `Services/`

Все изменения должны следовать принципам MVVM архитектуры.

---

**Примечание**: Этот проект создан для демонстрации возможностей SwiftUI и может быть использован как основа для реального приложения после подключения к бэкенду.

